# Backend API URL — no trailing slash
# In development this defaults to http://localhost:8000 if not set
# In production, set this to your Render backend URL e.g. https://gitflix-api.onrender.com
VITE_API_URL=http://localhost:8000
